export const postsStates$ = (state) => state.posts.data;
export const postStates$ = (state) => state.post.data;

export const modalState$ = (state) => state.modal;

export const modalUpdateState$ = (state) => state.modalUpdate;
